import styled from "styled-components";

const Basket = () => {
  return (
    <BasketComponent>
      <BasketSection>basket</BasketSection>
    </BasketComponent>
  );
};

const BasketComponent = styled.div``;

const BasketSection = styled.section``;

export default Basket;
