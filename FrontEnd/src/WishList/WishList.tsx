import styled from "styled-components";

const WishList = () => {
  return (
    <WishListComponent>
      <WishListSection>wishList</WishListSection>
    </WishListComponent>
  );
};

const WishListComponent = styled.div``;

const WishListSection = styled.section``;

export default WishList;
